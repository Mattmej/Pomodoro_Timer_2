// This will hold all the timer's components.

import React from 'react';
import Container from 'react-bootstrap/Container';
import '../App.css';

const timerBody = (props) => {
    return(
        <Container className='timerBody' />
    );
};

export default timerBody;